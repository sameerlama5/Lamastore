/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.imgur.com'], // Add all the domains you are fetching images from
    },
  };

export default nextConfig;
